import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Plastic Pallets | Hygienic Rackable and Nestable Pallet Options for Export Shipments',
  description: 'Choose lightweight nestable and durable rackable plastic pallets ideal for food, pharma, and export shipments.',
  alternates: { canonical: '/products/plastic-pallets/' },
}

export default function PlasticPalletsLayout({ children }: { children: React.ReactNode }) {
  return children
}
